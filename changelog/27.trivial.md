Made the doctests which hit the Zenodo API opt-in.

They are marked with the `# doctest: +ZENODO_API` directive
and are skipped unless the tests are run with `--zenodo-api-doctests`,
so a default run of the doctests neither waits on the network
nor needs a connection at all.
