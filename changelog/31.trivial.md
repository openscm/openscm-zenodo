Finished the live-API test suite: a full-lifecycle scenario test, the
restricted-access download path, and a weekly CI run against Zenodo so that a
change on their side becomes a failing build rather than a bug report.
Added `make test-integration` and `make test-integration-read-only`.
